# Frontend Environment Variables (Optional)
# All values have defaults - this file is not required for local development
#
# Server settings are in vite.config.mjs
# App config is in public/config.development.json (dev) and public/config.json (prod)

## Optional: Override server settings
# HOST=local.databridge.ch
# PORT=7005

## Optional: Override SSL certificates
# SSL_CRT_FILE=../certificates/server/server.crt
# SSL_KEY_FILE=../certificates/server/server.key
