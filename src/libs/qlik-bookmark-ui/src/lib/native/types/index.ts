import { TDataGridClientClasses } from '@libs/data-grid-ui'

export type TQlikBookmarkClasses = {
    iconButton?: string
    iconButtonDisabled?: string
    buttonSave?: string
    buttonCancel?: string
    buttonProgress?: string
    table?: Partial<TDataGridClientClasses>
}
